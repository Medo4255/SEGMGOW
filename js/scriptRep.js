document.getElementById('registrationForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Handle registration form submission
    // Implement logic to handle form submission, e.g., AJAX request
});

document.getElementById('donationPostForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Handle donation post form submission
    // Implement logic to handle form submission, e.g., AJAX request
});

document.getElementById('deletePostBtn').addEventListener('click', function() {
    // Handle delete button click
    // Implement logic to delete fulfilled donation post
});
